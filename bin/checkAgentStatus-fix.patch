--- PATCH FOR bin/specmem-console.cjs ---

This patch fixes the checkAgentStatus() function to use robust screen session detection.

1. Add import at top of file (after other requires):
   const { checkScreenSessionExists } = require('./screen-utils.cjs');

2. Replace the screen detection logic in checkAgentStatus():

   FIND (around line 2175-2179):
   ----------------------------------------
        // Check if screen session still exists
        const output = execSync(`screen -ls 2>/dev/null | grep "${agentId}" || true`, { encoding: 'utf8' });

        if (!output.includes(agentId)) {
   ----------------------------------------

   REPLACE WITH:
   ----------------------------------------
        // Check if screen session still exists using robust detection
        const sessionExists = checkScreenSessionExists(agentId);

        if (!sessionExists) {
   ----------------------------------------

That's it! The checkScreenSessionExists() function handles:
- Proper parsing of screen -ls output format
- Multiple agent naming patterns (team-member-X, agent-X, research-X, X)
- Regex-based session line extraction
- Error handling
